# DART API setup

1. Open `.env`.
2. Put your OPENDART authentication key after `DART_API_KEY=`.
3. Do not paste the key into `outputs/portfolio_dashboard.html`.

Example:

```env
DART_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

The key should stay local. The dashboard HTML should not contain API secrets.
